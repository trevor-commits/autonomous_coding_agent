# Audit — Phase 0 Cleanup Pass (2026-04-14)

> Historical archive note: this report records the repo state at audit time. Use current root docs and `design-history/README.md` to understand what has changed since then.

## Verdicts
- Prompt 1 (intent): GREEN — `PROJECT_INTENT.md` is complete, grounded in `canonical-architecture.md` Section 1, under 150 lines, and its sections match the requested structure.
- Prompt 2 (boundary): RED — the rewritten `STRUCTURE.md` is good, but repo-wide boundary cleanup is incomplete and its `Changes Propagated` appendix now contains stale line references.
- Prompt 3 (schemas): RED — all five schemas exist and are mostly well-formed, but `strategy-decision.schema.json` encodes the wrong required fields for two action variants.
- Prompt 4 (states): RED — the canonical section and ADR landed, but repo-wide normalization is incomplete because other markdown files still carry conflicting terminal-state vocabulary and companion docs still restate the rule instead of only pointing back to the canonical section.
- Cross-cutting: GREEN — the four task commits landed on the active branch, the commit scopes match the prompt scopes, and the separate work-log claims captured at audit time map to real file changes in `git log -p`.

## Findings
P1 — [FINAL-ARCHITECTURE-DECISION.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/design-history/FINAL-ARCHITECTURE-DECISION.md:43): historical docs still expose pre-normalization terminal-state vocabulary such as `READY / NOT_READY / BLOCKED`, `COMPLETE/BLOCKED`, and `{"action": "propose_terminal_state", "state": "COMPLETE"}` at lines 43, 51, and 122. This fails Prompt 4's explicit repo-wide grep check. Fix by normalizing these historical references or marking the document so it no longer presents conflicting state names as active model vocabulary.

P1 — [three-way-reconciliation-final.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/design-history/three-way-reconciliation-final.md:266): repo-wide boundary cleanup is incomplete. This historical doc still presents `.agent/contract.yml` and `.autoclaw/runs/<id>/` in an "IN REPO" storage model and still calls for creating a committed `.autoclaw/` directory structure at lines 266-281, 415, and 562. That fails Prompt 2's "search globally" requirement. Fix by rewriting or clearly quarantining these passages so they no longer describe current boundary truth.

P1 — [schemas/strategy-decision.schema.json](/Users/gillettes/Coding Projects/Autonomous Coding Agent/schemas/strategy-decision.schema.json:68): the `oneOf` requirements for two action variants are inverted. `checkpoint_candidate` currently requires `run_state` plus `reason`, while `propose_terminal_state` requires only `reason`. The canonical example shows `checkpoint_candidate` with just `reason`, and `propose_terminal_state` is the action that should carry `run_state`. Fix by making `checkpoint_candidate` require only `reason` and `propose_terminal_state` require both `run_state` and `reason`.

P2 — [STRUCTURE.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/STRUCTURE.md:35): the `Changes Propagated` appendix is no longer trustworthy after later edits shifted line numbers in the referenced files. For example, it still cites `RULES.md:268-269`, but the aligned memory-table update now lives at [RULES.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/RULES.md:277). Fix by refreshing the appendix to the current line numbers after the later Prompt 4 edits, or by rephrasing the appendix so it does not claim stable absolute lines.

P2 — [LOGIC.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/LOGIC.md:10): Prompt 4 required other docs to reference the canonical `Terminal States and Readiness Verdict` section rather than redefining it, but companion docs still restate the normalized rule directly. Examples include [LOGIC.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/LOGIC.md:10), [RULES.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/RULES.md:174), [PROMPTS.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/PROMPTS.md:881), and [IMPLEMENTATION-PLAN.md](/Users/gillettes/Coding Projects/Autonomous Coding Agent/IMPLEMENTATION-PLAN.md:564). Fix by collapsing duplicated explanatory restatements into short references to `canonical-architecture.md` Section 9.1, keeping only local enforcement text where the prompt explicitly requires it.

## Punch list (in fix order)
1. Normalize or quarantine stale historical markdown files that still use conflicting terminal-state names, starting with `FINAL-ARCHITECTURE-DECISION.md` and `three-way-reconciliation-final.md`.
2. Remove stale boundary language from historical docs that still imply `.autoclaw/` or `.agent/` live in the wrong place, then rerun the global `.agent/|.autoclaw/` grep.
3. Fix `schemas/strategy-decision.schema.json` so `checkpoint_candidate` and `propose_terminal_state` require the correct fields.
4. Reduce duplicated terminal-state definitions in `LOGIC.md`, `PROMPTS.md`, and `IMPLEMENTATION-PLAN.md` to canonical references; keep the RULES invariant but avoid parallel source-of-truth prose.
5. Refresh `STRUCTURE.md`'s `Changes Propagated` appendix so every cited `file:line` still resolves after the later docs pass.
6. Re-run the repo-wide terminal-state grep and boundary grep, then update the durable completion and evidence trail with the repair evidence.

## Evidence checked
- `git status -sb`
- `git branch --show-current`
- `git log -5 --oneline --decorate`
- `git log --all --oneline | head -20`
- `git show --stat --summary --name-only --format=fuller 26ac6e9`
- `git show --stat --summary --name-only --format=fuller e748ed3`
- `git show --stat --summary --name-only --format=fuller f7c1ef1`
- `git show --stat --summary --name-only --format=fuller af993be`
- `git show --format=medium --unified=3 26ac6e9 -- PROJECT_INTENT.md <historical work-log file at audit time>`
- `git show --format=medium --unified=3 e748ed3 -- STRUCTURE.md .gitignore canonical-architecture.md LOGIC.md RULES.md IMPLEMENTATION-PLAN.md <historical work-log file at audit time>`
- `git show --format=medium --unified=3 f7c1ef1 -- schemas <historical work-log file at audit time>`
- `git show --format=medium --unified=3 af993be -- canonical-architecture.md LOGIC.md RULES.md PROMPTS.md IMPLEMENTATION-PLAN.md schemas design-history/ADR-0001-terminal-state-normalization.md <historical work-log file at audit time>`
- `wc -l PROJECT_INTENT.md`
- `rg -n "TODO|verify|<placeholder>" PROJECT_INTENT.md`
- `rg -n "\\.agent/|\\.autoclaw/" -g '*.md'`
- `for term in READY NOT_READY COMPLETE BLOCKED UNSUPPORTED NEEDS_MORE_EVIDENCE; do rg -n "$term" -g '*.md'; done`
- `nl -ba PROJECT_INTENT.md | sed -n '1,120p'`
- `nl -ba STRUCTURE.md | sed -n '1,220p'`
- `nl -ba .gitignore`
- `ls -1 schemas`
- `python3 - <<'PY' ... json.load(...) for schemas/*.json ... PY`
- `nl -ba schemas/strategy-decision.schema.json | sed -n '1,220p'`
- `nl -ba schemas/readiness-report.schema.json | sed -n '1,220p'`
- `nl -ba canonical-architecture.md | sed -n '1,90p;216,232p;560,582p;385,395p;975,985p;1000,1008p'`
- `nl -ba RULES.md | sed -n '170,185p;260,285p'`
- `nl -ba LOGIC.md | sed -n '130,178p'`
- `nl -ba IMPLEMENTATION-PLAN.md | sed -n '260,285p;455,465p;580,590p'`
- `nl -ba FINAL-ARCHITECTURE-DECISION.md | sed -n '38,55p;118,140p'`
- `nl -ba three-way-reconciliation-final.md | sed -n '260,282p;404,416p;558,566p'`
- `nl -ba design-history/ADR-0001-terminal-state-normalization.md | sed -n '1,120p'`
