{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://autonomous-coding-agent.local/schemas/strategy-decision.schema.json",
  "title": "Strategy Decision Schema",
  "description": "Schema for typed StrategyDecision outputs returned by get_strategy_decision(...) in canonical-architecture.md Sections 3.2, 9.3, and 11, and consumed by IMPLEMENTATION-PLAN.md Phase 2 and Phase 4. The smallest v1 action graph is intentionally narrower than the historical checkpoint-oriented drafts: failure fingerprinting is supervisor-owned, and rollback/checkpoint actions are deferred until later hardening if they ever return as first-class surfaces.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "action"
  ],
  "properties": {
    "action": {
      "type": "string",
      "enum": [
        "collect_context",
        "request_builder_task",
        "run_contract_command",
        "launch_app",
        "stop_app",
        "run_ui_suite",
        "record_decision",
        "propose_terminal_state"
      ],
      "description": "Typed domain action selected by the strategy layer, from canonical-architecture.md Section 9.3 \"Typed Action Graph\"."
    },
    "description": {
      "type": "string",
      "description": "Builder task description. IMPLEMENTATION-PLAN.md uses request_builder_task with a description during manual strategy mode and automated BUILD routing."
    },
    "reason": {
      "type": "string",
      "description": "Human-readable reason for the decision or terminal-state proposal."
    },
    "name": {
      "type": "string",
      "description": "Contract-defined command name for run_contract_command, as shown in canonical-architecture.md Section 3.2 examples."
    },
    "scope": {
      "type": "string",
      "description": "Execution scope string for run_contract_command. The examples use targeted scope in canonical-architecture.md and PROMPTS.md."
    },
    "run_state": {
      "type": "string",
      "enum": [
        "COMPLETE",
        "BLOCKED",
        "UNSUPPORTED",
        "IN_PROGRESS"
      ],
      "description": "Supervisor-level run_state proposed through propose_terminal_state, aligned to canonical-architecture.md Section 9.1 \"Terminal States and Readiness Verdict\"."
    },
    "readiness_verdict": {
      "type": "string",
      "enum": [
        "READY",
        "NOT_READY",
        "NEEDS_MORE_EVIDENCE"
      ],
      "description": "Required alongside `run_state = COMPLETE` so the terminal proposal stays aligned with the final-gate contract."
    }
  },
  "oneOf": [
    {
      "properties": {
        "action": {
          "const": "run_contract_command"
        }
      },
      "required": [
        "action",
        "name"
      ]
    },
    {
      "properties": {
        "action": {
          "const": "request_builder_task"
        }
      },
      "required": [
        "action",
        "description"
      ]
    },
    {
      "description": "propose_terminal_state is the typed action family from canonical-architecture.md Section 9.3 that proposes a terminal run_state and therefore must include both the reason and the named run_state.",
      "properties": {
        "action": {
          "const": "propose_terminal_state"
        }
      },
      "required": [
        "action",
        "reason",
        "run_state"
      ]
    },
    {
      "properties": {
        "action": {
          "enum": [
            "collect_context",
            "launch_app",
            "stop_app",
            "run_ui_suite",
            "record_decision"
          ]
        }
      },
      "required": [
        "action"
      ]
    }
  ],
  "examples": [
    {
      "action": "run_contract_command",
      "name": "test",
      "scope": "targeted"
    },
    {
      "action": "request_builder_task",
      "description": "Implement the current milestone, then run the targeted verification commands named in the run contract."
    },
    {
      "action": "propose_terminal_state",
      "run_state": "BLOCKED",
      "reason": "The same failure fingerprint exceeded the retry threshold."
    },
    {
      "action": "propose_terminal_state",
      "run_state": "COMPLETE",
      "readiness_verdict": "READY",
      "reason": "Authoritative checks passed at final gate."
    }
  ]
}
